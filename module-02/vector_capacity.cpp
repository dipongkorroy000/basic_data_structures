#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> v ={1, 2, 3,4};
    // cout << v.capacity() << endl;
    
    // v.push_back(10);
    // cout << v.capacity() << endl;
    
    // v.push_back(10);
    // cout << v.capacity() << endl;
    
    // v.push_back(10);
    // cout << v.capacity() << endl;
    
    // v.push_back(10);
    // cout << v.capacity() << endl;
    
    // v.push_back(10);
    // cout << v.capacity() << endl;
    // ----- capacity increase 1, 2, 4, 8, 16 --- increating double
    
    // vector<int> v;
    // v.push_back(10);
    // v.push_back(20);
    // v.push_back(30);

    // cout << v.size() << endl;
    // v.clear(); // size clear but value not clear 
    // cout << v.size() << endl;
    // cout << v[0] << " " << v[1] << " " << v[2] << endl;

    // v.resize(2); // vector size decrease
    // v.resize(7); // vector size increase
    
    // v.resize(7, 100); // vector size increase and add 100

    // for(int i =0; i< v.size(); i++){
    //     cout << v[i] << " ";
    // }

    // return 0;
}