#include <bits/stdc++.h>
using namespace std;

int main()
{
    list<int> listone = {10, 20, 30, 40, 20, 50};

    // cout << listone.front() << endl; // access head value
    // cout << listone.back() << endl;  // access tail value

    // cout << *next(listone.begin(), 2) << endl; // access value using index pointer

    // cout << *listone.begin() << endl;

    return 0;
}